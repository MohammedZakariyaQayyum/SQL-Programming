-- This is a comment. Comments in SQL start with a dashdash
-- SQL is a language used to interact with databases and access data from databases.  

SELECT * FROM Customers; -- This line retrieves all info from the Customers table. The * is all info. A semicolon is placed afterwards to allow for more commands to be written.
SELECT CustomerName,City FROM Customers; -- This line returns the data from the fields, 'CustomerName' and 'City' in the 'Customers' database. The comma is used to separate the two fields wanted.
SELECT DISTINCT Country FROM Customers; -- Many fields have duplicated data. This line will return only different data from the 'Country' field.
SELECT COUNT(DISTINCT Country) FROM Customers; -- This line will return the number of differnet values from the 'Country' field.
